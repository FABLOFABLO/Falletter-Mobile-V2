import 'package:flutter/material.dart';

class FalletterAnswerView extends StatelessWidget {
  const FalletterAnswerView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('답변 페이지'),);
  }
}

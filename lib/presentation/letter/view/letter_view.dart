import 'package:flutter/material.dart';

class FalletterLetterView extends StatelessWidget {
  const FalletterLetterView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('레터 페이지'),);
  }
}

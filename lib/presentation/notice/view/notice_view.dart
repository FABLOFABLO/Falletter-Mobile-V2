import 'package:flutter/material.dart';

class FalletterNoticeView extends StatelessWidget {
  const FalletterNoticeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('알림 페이지'),);
  }
}
